<?php
if(isset($_POST['submit'])){
    $username = //finish this line
    $password = //finish this lin

    loginUser($email,$password);
}

function loginUser($email, $password){
    /*
        finish this funcion to check if username 
        and password from file match that which is passed from th form
    */
}

echo "HANDLE THIS PAGE";